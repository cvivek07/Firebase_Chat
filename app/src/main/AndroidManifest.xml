<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="google.firebase.firebasechatapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <!-- Defining Services -->



        <activity android:name=".GetUsername"
            android:label="@string/get_username">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OnlineUsers"
            android:label="@string/onlineusers"/>
        <activity android:name=".ChatPage"
            android:label="@string/chatpage">

        </activity>
    </application>

</manifest>